export class Video {

  id: number;

  name: string;

  filename: string;

  constructor(id: number, name: string, filename: string) {
    this.id = id;
    this.name = name;
    this.filename = filename;
  }
}
